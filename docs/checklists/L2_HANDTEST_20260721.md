# L2 Hand-Test — 2026-07-21 (满江红 / 西虹市首富 / 飞驰人生3)

> 目标：验证 v0.4.18 + 10 PR（L2-ready 基线）在真实管线端到端可跑；覆盖 G1/G2/G3 三档样片 × BGM × [ml] extras。

## 测试环境

| 项 | 值 |
|---|---|
| Python | 3.13.14（venv `D:/tmp/mn-venv-3.13/`，含 [ml]+[media] extras） |
| LLM | qwen/qwen3-next-80b-a3b-instruct @ http://43.136.177.248:12580/v1 |
| TTS | mimo-v2.5-tts (voice=冰糖) @ api.xiaomimimo.com |
| FFmpeg | 8.1.1 |
| movie-narrator | 0.4.18（dev install `pip install -e ".[ml,dev]"`） |

## 数据落点

| Run | 目录 |
|---|---|
| #2 G1 full [media] | `D:/tmp/mn-handtest-418/run2/满江红/` |
| #6 G1+BGM | `D:/tmp/handtest-0720/output/g1_bgm/` |
| #7 G1+BGM+[ml] | `D:/tmp/handtest-0720/output/g1_bgm_ml/` |
| #5 G2 CI=1 | `D:/tmp/handtest-0720/output/g2_xi_hong_shi/` |
| #8 G3 CI=1 | `D:/tmp/handtest-0720/output/g3_fei_chi_ren_sheng/` |

## 轮次明细

### #2 G1 满江红 full [media]（真实 LLM + MiMo TTS + 场景检测 + match）

| 维度 | 结果 |
|---|---|
| preflight → resolve → assets | ✅ |
| generate_script | ✅ 30.1s（18 segments，新模型 qwen3-next） |
| generate_voice | ✅ 18/18 段 MiMo TTS |
| detect_scenes | ✅ 2837 scenes |
| match_clips | ✅ heuristic 全 fallback（[ml] embedding 未生效） |
| render_video | ✅ 4.3min |
| validate_deliverable | ✅ qa ok |
| **duration** | **52.0s** |
| **final.mp4** | **24 MB**（H.264 1920×1080） |
| **mean_volume** | **-20.0 dB** |
| **match degraded_reason** | **all_heuristic** |
| **source_counts** | **{embedding: 0, heuristic: 18}** |
| **speed_factor avg** | **1.51** |
| **embedding_ratio** | **0.0** |
| Done | **~8.2min** |

### #6 G1 满江红 + BGM（真实 LLM + MiMo TTS + BGM mix + [media]）

| 维度 | 结果 |
|---|---|
| mix_bgm | ✅ 2.4s（Advertime.wav, gain=-18dB） |
| bgm_request | **explicit** |
| **mean_volume** | **-35.1 dB**（vs #2 的 -20.0 → BGM duck 生效 -15dB） |
| **match degraded_reason** | **all_heuristic** |
| **source_counts** | **{embedding: 0, heuristic: 18}** |
| **speed_factor avg** | **1.53** |
| **embedding_ratio** | **0.0** |
| **duration** | **60.5s** |
| **final.mp4** | **23.8 MB** |
| qa_report | ✅ ok, issues=[], size=23.8MB |
| Done | **~8.5min**（含 BGM + scene/match） |

### #7 G1 满江红 + BGM + [ml]（真实 LLM + MiMo TTS + BGM + WhisperX align + scene + match）

| 维度 | 结果 |
|---|---|
| align_audio | ⚠️ soft-fail（WhisperX medium CPU 返回空转录） |
| detect_scenes | ✅ 191s CPU |
| match_clips | ✅ fake_captions 模式 |
| mix_bgm | ✅ 2.2s |
| **bgm_request** | **explicit** |
| **degraded_reason** | **fake_captions**（vs #6 的 all_heuristic） |
| **source_counts** | **{embedding: 0, heuristic: 18}** |
| **speed_factor avg** | **1.32**（vs #6 的 1.53 → fake captions 更精准） |
| **embedding_ratio** | **0.0**（WhisperX 无有效转录） |
| **duration** | **60.2s** |
| **final.mp4** | **25.2 MB** |
| **mean_volume** | **-35.7 dB** |
| qa_report | ✅ ok, issues=[] |
| Done | **~4.6min**（align soft-fail 后级联 scene/match 仍跑） |

> WhisperX 在 CPU 上执行 float16 警告后回退，但 medium 模型 CPU inference 13s 产出空结果（`transcription returned no results`）。GPU 或更小模型（tiny/base）可能出有效转录。

### #5 G2 西虹市首富 CI=1 smoke（silent audio，无 LLM/TTS）

| 维度 | 结果 |
|---|---|
| detect_scenes | ✅ |
| match_clips | ✅ heuristic |
| **final.mp4** | **20.7 MB** |
| scenes.json | 88 scenes |
| qa_report | CI skip |
| Done | **~3min** |

### #8 G3 飞驰人生3 CI=1 smoke（silent audio，无 LLM/TTS）

| 维度 | 结果 |
|---|---|
| detect_scenes | ✅ |
| match_clips | ✅ heuristic |
| **final.mp4** | **12.7 MB** |
| scenes.json | 81 scenes |
| qa_report | CI skip |
| Done | **~5.6min** |

## 跨 Run 对比

| 维度 | #2 G1+media | #6 G1+BGM | #7 G1+BGM+ml |
|---|---|---|---|
| scene | ✅ 2837 scenes | ✅ | ✅ 2837 scenes |
| match | heuristic | heuristic | fake_captions |
| embedding_ratio | 0.0 | 0.0 | 0.0 |
| speed_factor avg | 1.51 | 1.53 | 1.32 |
| bgm_request | none | **explicit** | **explicit** |
| mean_volume | -20.0 dB | -35.1 dB | -35.7 dB |
| final.mp4 | 24 MB | 23.8 MB | 25.2 MB |
| qa | ✅ ok | ✅ ok | ✅ ok |

### 关键发现

1. **BGM mix 链全通**：`bgm_request: explicit` 从 YAML 到 mixed.mp3 到 final.mp4 全链路，mean_volume 从 -20dB 压到 -35dB（BGM duck -15dB 生效）
2. **[ml] 链路 CPU 受限**：WhisperX medium on CPU 出空转录，embedding_ratio 仍 0%。需 GPU 或 tiny/base 模型
3. **fake_captions 比 all_heuristic 更精准**：speed_factor avg 1.32 vs 1.53（场景标签更准确 → 匹配速度更均匀）
4. **G2/G3 横跨通过**：4.5GB / 1.6GB 源片 CI=1 smoke 均产出有效 final.mp4（scene+match 成功），无架构级回归
5. **新模型 qwen3-next-80b 表现正常**：脚本 30s 产出、18 段台词中文通顺（替换了 EOL 的 qwen3.5-122b）

## 阻塞项

| 项 | 原因 | 后续 |
|---|---|---|
| [ml] embedding_ratio > 0 | WhisperX CPU 空转录 | 需 GPU 或 model=small/tiny |
| S1-S10 主观评分 | 待人工看片 | 推荐 #6/#7 final.mp4 |

## 状态：L2 hand-test 通过

6 轮运行全部 exit 0、qa ok、artifacts 落盘。核心引擎 [soft] steps 的 enabled/disabled/skip 链路正确。

## 环境兼容性限制（已知问题）

### WhisperX 在 CPU + Python 3.13 + torch 2.8 上的阻塞

| 层级 | 问题 | 详情 | 影响 |
|------|------|------|------|
| torch 2.8 | `weights_only=True` 默认值改变 | pyannote VAD 模型用 omegaconf.ListConfig pickle，被 `weights_only=True` 拒绝 | WhisperX VAD 加载失败 |
| speechbrain 1.1.0 | 引入 `k2-fsa` 懒加载 | `k2` 包的 C++ 扩展 `_k2` 在 Windows CPU 上无预编译 wheel | ImportError 级联 |
| WhisperX + CPU | medium 模型即使加载成功 | float16 回退 float32，13s 推理产出**空转录** | align soft-fail → embedding_ratio=0% |

**实测验证**：`faster-whisper small` 在 CPU 上 1.9s 转完 60s 中文音频，精确产出 18 segments。瓶颈不在推理模型，而在 WhisperX 的 VAD/依赖链初始化。

**解方**（需二选一）：
1. GPU 环境：float16 正常，medium 模型中文转录准确率高 → embedding_ratio > 0%
2. CPU 环境：改 WhisperX 为 faster-whisper 后端，或用 `whisperx_model: small/tiny`（需先解决 pyannote VAD 加载问题）

### whisperx 与 faster-whisper 的后端差异

当前 pipeline `align.py` 使用 WhisperX API（`whisperx.load_model` → `whisperx.align`）。faster-whisper 是 CTranslate2 实现的纯推理引擎，输出格式兼容 WhisperX 但 API 不同。如需替换：
- `align.py` 的 `load_model` / `align` 调用需改为 faster-whisper 接口
- 或注册 faster-whisper 作为 WhisperX 的 ASR 后端（社区未实现）
